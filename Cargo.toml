[package]
name = "mercyshieldplus"
version = "0.1.0"
edition = "2021"
description = "Proprietary Foolproof Quantum Fortress Core Eternal ⚡️"
license = "MIT"
repository = "https://github.com/Eternally-Thriving-Grandmasterism/MercyShieldPlus"

[lib]
name = "mercyshieldplus"
crate-type = ["cdylib", "staticlib"]  # Android native + static mercy

[dependencies]
bevy = { version = "0.13", features = ["png"] }  # or 0.14 latest
# Minimal no external yet — custom math novel transcription next
# Future: zero-deps pure std for ultimate foolproof

[features]
default = []
