@Composable
fun AnomalyBurst(isActive: Boolean) {
    val scale by animateFloatAsState(if (isActive) 1.5f else 1f)
    Box(modifier = Modifier.scale(scale).background(Color.Red.copy(alpha = 0.5f), CircleShape).size(200.dp))
}
